# Agentic RAG Schedule Assistant

Gemini + ChromaDB + RAG + tool calling + FastAPI.

Tools: get_schedule, update_schedule.

Render build command: pip install -r requirements.txt
Render start command: uvicorn app:app --host 0.0.0.0 --port $PORT

Required environment variable: GOOGLE_API_KEY
